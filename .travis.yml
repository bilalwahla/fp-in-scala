language:
  scala

jdk:
- oraclejdk8

notifications:
  email:
  - bilalwahla@gmail.com
  on_success: always
  on_failure: always

branches:
  only:
  - master

script:
  - sbt clean coverage test coverageReport

after_success:
- bash <(curl -s https://codecov.io/bash)